function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <h1>Inukki</h1>
        <p>iOS and Android shell is now wired with Capacitor.</p>
        <p>Next: run install, build, and sync to native projects.</p>
      </section>
    </main>
  );
}

export default App;
